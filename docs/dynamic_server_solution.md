# Решение для динамических IP серверов Ollama

Данный документ описывает, как настроить AILLM для работы с Ollama сервером, у которого динамический IP адрес.

---

## 📋 Оглавление

1. [Проблема](#проблема)
2. [Решения](#решения)
   - [mDNS (Рекомендуется)](#-1-mdns-рекомендуется)
   - [Fallback URLs](#-2-fallback-urls)
   - [Авто-обнаружение](#-3-авто-обнаружение)
3. [Настройка сервера Ollama](#настройка-сервера-ollama)
4. [Настройка AILLM](#настройка-aillm)
5. [Проверка работы](#проверка-работы)
6. [Troubleshooting](#troubleshooting)

---

## Проблема

При использовании DHCP IP-адрес сервера Ollama может меняться после перезагрузки или по истечении аренды. Это приводит к тому, что AILLM не может подключиться к серверу.

```
❌ Проблема: IP 192.168.178.126 → После перезагрузки → IP 192.168.178.142
   AILLM всё ещё ищет 192.168.178.126 → Ошибка подключения
```

---

## Решения

### 🏷️ 1. mDNS (Рекомендуется)

**mDNS (Multicast DNS)** позволяет обращаться к серверу по имени вместо IP. Имя остаётся постоянным независимо от IP.

#### Преимущества:
- ✅ Работает автоматически
- ✅ Не требует DNS сервера
- ✅ Поддерживается в macOS, Linux, Windows

#### Как это работает:
```
ollama-server.local → mDNS запрос → 192.168.178.??? → Подключение!
```

---

### 🔄 2. Fallback URLs

Система автоматически пробует альтернативные адреса, если основной недоступен.

```yaml
# config.yaml
ollama:
  base_url: http://192.168.178.126:11434
  fallback_urls:
    - ollama-server.local:11434
    - ollama.local:11434
    - 192.168.178.200:11434
```

#### Логика:
```
base_url недоступен?
    → Попробовать fallback_urls[0]
        → Недоступен? → fallback_urls[1]
            → Недоступен? → fallback_urls[2]
                → Использовать найденный
```

---

### 🔍 3. Авто-обнаружение

Система автоматически сканирует сеть в поисках Ollama сервера.

```yaml
# config.yaml
ollama:
  auto_discover: true
```

#### Что проверяется:
1. mDNS имена: `ollama.local`, `ollama-server.local`
2. Hostnames: `ollama`, `ollama-server`
3. Популярные IP в подсети: `*.1`, `*.100`, `*.126`, `*.200`, `*.254`

---

## Настройка сервера Ollama

### Ubuntu/Debian

```bash
# 1. Установить Avahi (mDNS daemon)
sudo apt update
sudo apt install avahi-daemon avahi-utils

# 2. Установить hostname
sudo hostnamectl set-hostname ollama-server

# 3. Перезапустить Avahi
sudo systemctl restart avahi-daemon

# 4. Проверить что сервис работает
sudo systemctl status avahi-daemon

# 5. Проверить mDNS имя
avahi-resolve -n ollama-server.local
```

### Arch Linux

```bash
sudo pacman -S avahi nss-mdns
sudo systemctl enable --now avahi-daemon
sudo hostnamectl set-hostname ollama-server
```

### macOS

macOS поддерживает mDNS (Bonjour) из коробки:

```bash
# Установить hostname
sudo scutil --set HostName ollama-server
sudo scutil --set LocalHostName ollama-server

# Перезагрузить
sudo killall -HUP mDNSResponder
```

### Windows

```powershell
# Установить hostname
Rename-Computer -NewName "ollama-server" -Restart
```

Windows поддерживает mDNS через Bonjour (устанавливается с iTunes) или через WSL.

---

## Настройка AILLM

### Вариант 1: Только mDNS (простой)

```yaml
# backend/config/config.yaml
llm:
  providers:
    ollama:
      enabled: true
      base_url: http://ollama-server.local:11434
      timeout: 300
```

### Вариант 2: С fallback (надёжный)

```yaml
# backend/config/config.yaml
llm:
  providers:
    ollama:
      enabled: true
      base_url: http://ollama-server.local:11434
      fallback_urls:
        - ollama.local:11434
        - 192.168.178.126:11434  # Последний известный IP
      timeout: 300
```

### Вариант 3: Полная автоматика

```yaml
# backend/config/config.yaml
llm:
  providers:
    ollama:
      enabled: true
      base_url: http://ollama-server.local:11434
      fallback_urls:
        - ollama.local:11434
      auto_discover: true  # Искать в сети если ничего не работает
      timeout: 300
```

### Через переменную окружения

```bash
# Установить перед запуском AILLM
export OLLAMA_HOST=ollama-server.local:11434

# Или с IP (если известен)
export OLLAMA_HOST=192.168.178.126:11434
```

---

## Проверка работы

### 1. Проверить mDNS на клиенте

**Linux:**
```bash
# Установить avahi-utils если нет
sudo apt install avahi-utils

# Проверить резолв
avahi-resolve -n ollama-server.local
# Должно показать: ollama-server.local 192.168.178.XXX
```

**macOS:**
```bash
ping ollama-server.local
# или
dns-sd -G v4 ollama-server.local
```

**Windows:**
```powershell
ping ollama-server.local
```

### 2. Проверить Ollama API

```bash
# Через mDNS имя
curl http://ollama-server.local:11434/api/tags

# Должен вернуть список моделей
```

### 3. Проверить AILLM

```bash
# Запустить backend
cd /path/to/AILLM
python -m backend.main

# В логах должно быть:
# INFO: Using Ollama URL from config: http://ollama-server.local:11434
# INFO: Ollama server (http://ollama-server.local:11434): ~24 GB VRAM estimated
```

---

## Troubleshooting

### mDNS имя не резолвится

**Проблема:** `ping ollama-server.local` не работает

**Решения:**

1. **Проверить Avahi на сервере:**
   ```bash
   sudo systemctl status avahi-daemon
   ```

2. **Проверить firewall:**
   ```bash
   # mDNS использует порт 5353/UDP
   sudo ufw allow 5353/udp
   ```

3. **На клиенте (Linux) установить nss-mdns:**
   ```bash
   sudo apt install libnss-mdns
   
   # Проверить /etc/nsswitch.conf содержит:
   # hosts: files mdns4_minimal [NOTFOUND=return] dns
   ```

### Ollama недоступен по сети

**Проблема:** Ollama работает, но не отвечает с других машин

**Решения:**

1. **Ollama должен слушать на 0.0.0.0:**
   ```bash
   # Установить переменную перед запуском Ollama
   export OLLAMA_HOST=0.0.0.0:11434
   ollama serve
   
   # Или в systemd service:
   # Environment="OLLAMA_HOST=0.0.0.0:11434"
   ```

2. **Открыть порт в firewall:**
   ```bash
   sudo ufw allow 11434/tcp
   ```

### Авто-обнаружение не находит сервер

**Проблема:** `auto_discover: true` не находит Ollama

**Решения:**

1. **Убедиться что Ollama слушает на 0.0.0.0**

2. **Проверить что сервер в той же подсети:**
   ```bash
   # На клиенте
   ip addr | grep inet
   # 192.168.178.10
   
   # На сервере  
   ip addr | grep inet
   # 192.168.178.126
   
   # Первые 3 октета должны совпадать
   ```

3. **Firewall может блокировать сканирование**

---

## Рекомендуемая конфигурация

Для максимальной надёжности используйте комбинацию всех методов:

```yaml
# backend/config/config.yaml
llm:
  default_provider: ollama
  providers:
    ollama:
      enabled: true
      
      # Основной URL - mDNS имя (не IP!)
      base_url: http://ollama-server.local:11434
      
      # Резервные варианты
      fallback_urls:
        - ollama.local:11434          # Альтернативное mDNS
        - 192.168.178.126:11434       # Последний известный IP
        - 192.168.178.1:11434         # Роутер (если там Ollama)
      
      # Авто-поиск как последнее средство
      auto_discover: true
      
      timeout: 300
      max_retries: 2
      cache_enabled: true
      auto_detect_models: true
```

---

## Статический IP как альтернатива

Если mDNS не подходит, можно настроить статический IP на сервере:

### Ubuntu/Debian (Netplan)

```yaml
# /etc/netplan/01-static.yaml
network:
  version: 2
  ethernets:
    eth0:
      addresses:
        - 192.168.178.126/24
      gateway4: 192.168.178.1
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
```

```bash
sudo netplan apply
```

### Резервирование DHCP на роутере

Большинство роутеров позволяют "зарезервировать" IP для конкретного MAC-адреса:

1. Зайти в настройки роутера
2. Найти раздел DHCP → Address Reservation
3. Добавить MAC-адрес сервера с желаемым IP

---

## Архитектура решения

```
┌─────────────────────────────────────────────────────────────────┐
│                         AILLM Backend                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   ComplexityAnalyzer                      │  │
│  │                                                           │  │
│  │  _get_ollama_url()                                        │  │
│  │      │                                                    │  │
│  │      ├─→ Переменная окружения OLLAMA_HOST                 │  │
│  │      │                                                    │  │
│  │      ├─→ config.yaml: base_url                            │  │
│  │      │       │                                            │  │
│  │      │       └─→ _resolve_ollama_url()                    │  │
│  │      │               │                                    │  │
│  │      │               ├─→ DNS/mDNS резолв                  │  │
│  │      │               └─→ Проверка доступности API         │  │
│  │      │                                                    │  │
│  │      ├─→ config.yaml: fallback_urls[]                     │  │
│  │      │       │                                            │  │
│  │      │       └─→ Пробовать каждый по очереди              │  │
│  │      │                                                    │  │
│  │      └─→ auto_discover: true                              │  │
│  │              │                                            │  │
│  │              ├─→ mDNS имена (ollama.local, etc)           │  │
│  │              ├─→ Hostnames (ollama, ollama-server)        │  │
│  │              └─→ Сканирование подсети                     │  │
│  │                                                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│                    Ollama сервер найден!                        │
│                    192.168.178.XXX:11434                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Полезные команды

```bash
# Найти Ollama серверы в сети (Linux)
avahi-browse -rt _http._tcp | grep ollama

# Проверить открытые порты 11434 в сети
nmap -p 11434 192.168.178.0/24

# Узнать свой IP
ip addr | grep "inet " | grep -v 127.0.0.1

# Проверить Ollama API
curl -s http://ollama-server.local:11434/api/tags | jq '.models[].name'
```

---

*Последнее обновление: декабрь 2025*

