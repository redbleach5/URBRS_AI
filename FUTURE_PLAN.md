# План будущих улучшений

## 📊 Оценка системы (декабрь 2025)

**Общий уровень:** Очень продвинутая система (8.5/10)

**Сильные стороны:**
- ✅ ReflectionMixin — превосходная система самокоррекции агентов
- ✅ LearningSystem — полноценное персистентное обучение на опыте
- ✅ IntelligentModelRouter — умный динамический скоринг моделей
- ✅ RAG система — мощная, с суммированием и кэшированием
- ✅ Inter-Agent Communication — делегирование и broadcast
- ✅ Performance Tracking — детальные метрики с историей
- ✅ Safety Guard — защита от опасных операций
- ✅ Code Intelligence — AST-анализ, семантический поиск кода
- ✅ Self-Consistency — голосование между N ответами
- ✅ Multi-Agent Synthesis — объединение результатов агентов

---

## 🚀 Стратегические идеи развития

### Архитектура и агенты

| Идея | Суть | Сложность | Импакт | Приоритет |
|------|------|-----------|--------|-----------|
| **Agent Swarm** | Динамическая сборка "роя" микро-агентов под задачу | 🔴 Высокая | 🚀🚀🚀 | Phase 3 |
| **Tool Discovery** | Агент изучает API и генерирует Tools на лету (по Swagger) | 🔴 Высокая | 🚀🚀 | Phase 3 |
| **Мультимодальный RAG** | RAG для изображений, диаграмм, скриншотов кода | 🟡 Средняя | 🚀🚀 | Phase 2 |

### Интеллект и автономность

| Идея | Суть | Сложность | Импакт | Приоритет |
|------|------|-----------|--------|-----------|
| **Проактивность** | Система сама предлагает рефакторинг, исправления, обновления | 🟡 Средняя | 🚀🚀🚀 | Phase 2 |
| ~~**Гибридный роутер**~~ ✅ | Выбор модели по трилемме cost/quality/privacy | 🟢 Низкая | 🚀🚀 | Phase 1 |
| ~~**Встроенный тестер**~~ ✅ | Автозапуск тестов, симуляция, верификация кода | 🟡 Средняя | 🚀🚀🚀 | Phase 1 |

### Интеграция и экосистема

| Идея | Суть | Сложность | Импакт | Приоритет |
|------|------|-----------|--------|-----------|
| **MCP интеграция** | Поддержка Model Context Protocol (Anthropic) | 🟢 Низкая | 🚀🚀 | Phase 1 |
| **Agent-as-a-Service** | API для внешних приложений (IDE, CI/CD, боты) | 🟡 Средняя | 🚀🚀 | Phase 2 |
| **Evals/бенчмарки** | Автоматическая оценка на SWE-bench, AgentBench | 🟢 Низкая | 🚀 | Phase 2 |

---

## 🔴 Дизайн-проблемы (высокий приоритет)

### 1. ~~Консолидация систем роутинга моделей~~ ✅ РЕШЕНО (28 декабря 2025)
**Проблема:** 5 компонентов с пересекающейся функциональностью.

**Решение:** Создан `UnifiedModelRouter` — единая точка входа:
```python
from backend.core.unified_model_router import UnifiedModelRouter

router = UnifiedModelRouter(config)
selection = await router.select_model(task, task_type="code", complexity="medium")
# Возвращает: model, server_url, provider, scores, fallbacks
```

### 2. ~~Single Responsibility в агентах~~ ✅ РЕШЕНО (26 декабря 2025)
Вынесено в отдельные модули:
- ✅ `text_utils.py` — `extract_code_from_markdown()`, `detect_language_from_task()`
- ✅ `TwoStageProcessor` — двухстадийная обработка
- ✅ `CodeWriterAgent` теперь использует эти утилиты

### 3. Отсутствие интерфейсов между компонентами
**Проблема:** Прямые зависимости между модулями затрудняют тестирование.

**Решение:** Добавлены ABC интерфейсы:
- ✅ `BaseLLMProvider` — для LLM провайдеров
- ✅ `BaseAgent` — для агентов
- ✅ `BaseTool` — для инструментов
- 🔄 `IModelRouter` — добавлен в `UnifiedModelRouter`

### 4. ~~Streaming для всех агентов~~ ✅ РЕШЕНО (28 декабря 2025)
**Решение:** Добавлен `execute_stream()` в `BaseAgent`:
```python
async for chunk in agent.execute_stream(task, context):
    print(chunk, end="", flush=True)
```

### 5. ~~Проверка на галлюцинации (FactChecker)~~ ✅ РЕШЕНО (3 января 2026)
**Проблема:** LLM может генерировать неточную информацию без проверки.

**Решение:** Создан `FactCheckerMixin` в `backend/agents/fact_checker_mixin.py`:
```python
from backend.agents import FactCheckerMixin, FactCheckResult

class MyAgent(BaseAgent, FactCheckerMixin):
    async def execute(self, task, context):
        result = await super().execute(task, context)
        # Проверяем факты через RAG и web search
        fact_result = await self.check_facts(result["response"], task)
        if fact_result.needs_correction:
            result["response"] = fact_result.corrected_response
        return result
```

### 6. Chat и IDE не делят контекст проекта
**Проблема:** Вкладки работают изолированно. Chat не знает о `projectPath` из IDE.  
**Решение:** Создать общий `ProjectStore` (Zustand) для `projectPath` и `projectTree`  
**Сложность:** Средняя (2-3 часа)

### 7. ~~LongTermMemory не полностью интегрирована~~ ✅ РЕШЕНО (3 января 2026)
**Проблема:** Память сохраняет решения и feedback, но не используется для персонализации, выбора модели/агента, избежания повторных ошибок.  
**Решение:** Интегрировано в Chat API:
- `get_personalization_prompt()` — персонализация на основе предпочтений пользователя
- `get_error_avoidance_prompt()` — предупреждения о похожих ошибках в прошлом
- Данные добавляются в system prompt автоматически

### 8. Агенты не обмениваются результатами
**Проблема:** При мульти-агентном выполнении каждый агент работает изолированно. Результаты просто объединяются, но не синтезируются.  
**Решение:** ✅ Создан `MultiAgentSynthesis` — стратегии merge, select_best, consensus, debate

---

## 🟡 Качество кода (средний приоритет)

### 6. ~~Дублирование логики сложности~~ ✅ РЕШЕНО (28 декабря 2025)
**Проблема:** Анализ сложности дублировался в 5 местах.

**Решение:** Создан единый `TaskComplexityService`:
```python
from backend.core.task_complexity_service import get_complexity_service

service = get_complexity_service()
result = service.analyze(task, task_type="code")
# result.level, result.estimated_minutes, result.recommended_tier
```

Все компоненты теперь используют этот сервис.

### 7. Улучшение тестового покрытия
**Текущее состояние:** 61 тест в 12 файлах (~400 LOC)

Добавить интеграционные тесты для:
- [ ] Полного flow от запроса до ответа
- [ ] Fallback механизмов между серверами
- [ ] Race conditions в discovery
- [ ] Multi-agent collaboration
- [ ] Learning system persistence
- [ ] UnifiedModelRouter
- [ ] TaskComplexityService

### 8. Логирование с контекстом
**Проблема:** Сложно отслеживать запросы через все компоненты.

**Решение:** Добавить correlation ID:
```python
class RequestContext:
    correlation_id: str = field(default_factory=lambda: str(uuid.uuid4()))
    
    def get_logger(self, name: str) -> Logger:
        return logger.bind(correlation_id=self.correlation_id)
```

### 9. Приоритетная очередь задач
**Проблема:** Все задачи выполняются в порядке поступления.

**Решение:** Добавить priority queue:
```python
class PriorityTaskQueue:
    async def enqueue(self, task: Task, priority: TaskPriority):
        # Учитывать: сложность, срочность, зависимости
        
    async def dequeue(self) -> Task:
        # Возвращать задачу с наивысшим приоритетом
```

### 10. ~~Адаптивная температура~~ ✅ РЕШЕНО (3 января 2026)
**Проблема:** Фиксированная температура для всех типов задач.

**Решение:** Создан `backend/core/adaptive_temperature.py`:
```python
from backend.core.adaptive_temperature import get_optimal_temperature

temp = get_optimal_temperature(task="напиши функцию", mode="ide")
# -> 0.1 (низкая для кода)

temp = get_optimal_temperature(task="придумай историю", mode="chat")
# -> 0.8 (высокая для креатива)
```
Интегрировано в Chat API — температура выбирается автоматически.

### 11. Монолитный компонент UnifiedChat.tsx (2278 строк)
**Проблема:** Нарушает Single Responsibility: рендеринг, markdown, выполнение кода, состояние, модели.  
**Решение:** Разбить на `ChatMessage.tsx`, `CodeExecutor.tsx`, `MarkdownRenderer.tsx`, `ModelSelector.tsx`, `ConversationSidebar.tsx`

### 12. Дублирование логики парсинга кода
**Проблема:** Три функции с пересекающейся логикой: `extractCodeFromMarkdown()`, `parseCodeBlocks()`, `detectCodeType()`.  
**Решение:** Объединить в класс `CodeParser` с методами `extract()`, `detectType()`, `validate()`

### 13. Смешанная локализация (RU/EN)
**Проблема:** Интерфейс содержит смесь языков: `'Ассистент', 'Задачи', 'Agents'`.  
**Решение:** Использовать i18n библиотеку (react-intl, react-i18next)

### 14. ~~История чата ограничена 10 сообщениями~~ ✅ РЕШЕНО (3 января 2026)
**Проблема:** Используются только последние 10 сообщений. Модель "забывает" контекст в длинных разговорах.  
**Решение:** Создан `ChatSummarizer` в `backend/core/chat_summarizer.py`:
- Автоматическое суммирование старых сообщений через LLM
- Извлечение ключевых тем, требований, решений
- Кэширование summary по conversation_id
- Интеграция в Chat API

---

## 🟢 Оптимизации (низкий приоритет)

### 11. Кэширование профилей моделей
`ModelProfile.from_model_name()` вызывается часто — добавить LRU-кэш:
```python
from functools import lru_cache

@lru_cache(maxsize=256)
def from_model_name(cls, name: str) -> 'ModelProfile':
    ...
```

### 12. Ленивая инициализация компонентов
Некоторые компоненты можно инициализировать по требованию вместо старта.

### 13. Метрики и мониторинг
- Prometheus метрики для времени ответа моделей
- Grafana дашборд для визуализации производительности
- Alerting для критических метрик

### 14. A/B тестирование моделей
```python
class ModelABTester:
    async def run_experiment(
        self, 
        task: str, 
        models: List[str], 
        evaluation_fn: Callable
    ) -> ABTestResult:
        # Запуск одной задачи на нескольких моделях
        # Автоматическое сравнение результатов
```

### 15. Утечка памяти через blob URL
**Файл:** `frontend/src/components/UnifiedChat.tsx:710-719`  
**Проблема:** Cleanup blob URL вызывается только при unmount. При множественных запусках URL накапливаются.  
**Решение:** Добавить cleanup в зависимости от `codeExecutionResults`

### 16. WebSocket для прогресса
**Проблема:** В `backend/main.py` есть endpoint `/ws`, но фронтенд использует только REST. Нет real-time обновлений статуса.  
**Решение:** Реализовать WebSocket клиент для получения прогресса длинных задач

### 17. Обработка offline режима
**Проблема:** При потере сети только общее сообщение об ошибке. Нет кэширования, retry, индикатора offline.  
**Решение:** Добавить Service Worker и offline-first подход

---

## ⚡ Оптимизации производительности (быстрые модели)

Быстрые модели (qwen2.5:1.5b, llama3.2:1b, gemma2:2b) могут ускорить систему на **30-50%**.

### Уже используют быстрые модели:
- ✅ `LLMClassifier` (`backend/core/llm_classifier.py`)
- ✅ `TwoStageProcessor` (`backend/core/two_stage_processor.py`)

### Рекомендуется добавить:

#### 18. Декомпозиция задач — быстрая модель
**Файл:** `backend/orchestrator.py:383-442`  
**Проблема:** Используется полная модель с `thinking_mode=True` для всех задач.  
**Решение:** Быстрая модель определяет простую/сложную задачу, полная только для сложных  
**Выигрыш:** ~70% задач простые, экономия 1-3 сек/задачу

#### 19. Быстрая рефлексия агентов
**Файл:** `backend/agents/reflection_mixin.py`  
**Проблема:** Полная модель для оценки качества результата.  
**Решение:** Быстрая предварительная оценка, полная рефлексия только для проблемных результатов  
**Выигрыш:** Пропуск рефлексии для ~60% хороших результатов, экономия 2-5 сек

#### 20. Быстрая маршрутизация задач
**Файл:** `backend/core/task_router.py:73-126`  
**Проблема:** Стандартная модель для классификации типа/сложности задачи.  
**Решение:** Интегрировать `LLMClassifier` для использования быстрой модели  
**Выигрыш:** Экономия 0.5-1 сек/задачу

#### 21. Предварительная фильтрация в Chat API
**Файл:** `backend/api/routers/chat.py`  
**Проблема:** Все сообщения обрабатываются одинаково.  
**Решение:** Быстрая классификация (greeting/question/task), простые сообщения — быстрая модель  
**Выигрыш:** ~20% сообщений простые, экономия 2-5 сек/сообщение

#### 22. TwoStageProcessor для других агентов
**Файл:** `backend/core/two_stage_processor.py`  
**Проблема:** Используется только в `code_writer.py`.  
**Решение:** Интегрировать в `ResearchAgent`, `DataAnalysisAgent`, `SmartProjectAnalyzer`

### 📊 Потенциальная экономия:

| Оптимизация | Частота | Экономия | Выигрыш |
|-------------|---------|----------|---------|
| Декомпозиция (простые) | 70% | 1-3 сек | ~2 сек/задачу |
| Пропуск рефлексии | 60% | 2-5 сек | ~2 сек/задачу |
| Быстрые greeting | 20% | 2-5 сек | ~0.6 сек/сообщение |
| Быстрая маршрутизация | 100% | 0.5-1 сек | ~0.7 сек/задачу |

---

## 🚀 Новые возможности (roadmap)

### Phase 1: Быстрые победы (1-2 недели)

#### 23. Гибридный AI-роутер (Cost/Quality/Privacy)
**Расширить** `UnifiedModelRouter` параметрами:
```python
@dataclass
class RoutingPolicy:
    prefer_local: bool = True       # Приватность: локальные модели в приоритете
    max_cost_tier: int = 3          # Бюджет: 1=free, 2=cheap, 3=premium
    quality_threshold: float = 0.7  # Минимальное качество
    
selection = await router.select_model(
    task, 
    policy=RoutingPolicy(prefer_local=True, max_cost_tier=2)
)
# Приватный код → Ollama, документация → Claude
```

#### 24. Встроенный симулятор и тестировщик
```python
class CodeTester:
    async def test_code(self, code: str, language: str) -> TestResult:
        # 1. Запуск unit-тестов (pytest/jest)
        # 2. Статический анализ (ruff/eslint)
        # 3. Симуляция выполнения в sandbox
        # 4. Оценка корректности

# Интеграция в CodeWriterAgent:
result = await agent.execute(task)
test_result = await code_tester.test_code(result.code, "python")
if not test_result.passed:
    result = await agent.fix_code(result.code, test_result.errors)
```

#### 25. MCP интеграция (Model Context Protocol)
```python
class MCPConnector:
    """Подключение к экосистеме MCP-серверов"""
    
    async def connect_resource(self, uri: str) -> MCPResource:
        # Подключение к Jira, Figma, PostgreSQL, etc.
        
    async def list_tools(self) -> List[MCPTool]:
        # Доступные инструменты из MCP-серверов
```

### Phase 2: Интеллект и автономность (3-4 недели)

#### 26. Проактивный ассистент
```python
class ProactiveAnalyzer:
    async def analyze_codebase(self, path: str) -> List[Suggestion]:
        # 1. Сканирование кодовой базы
        # 2. Обнаружение code smells, устаревших зависимостей
        # 3. Предложение рефакторинга
        # 4. Уведомление о потенциальных багах
        
    async def schedule_analysis(self, interval: str = "daily"):
        # Периодический анализ по расписанию
```

#### 27. Мультимодальный RAG
```python
class MultimodalRAG:
    async def index_image(self, image_path: str) -> None:
        # Индексация диаграмм, скриншотов, архитектурных схем
        
    async def search_visual(self, query: str) -> List[VisualResult]:
        # Поиск по изображениям
        
    async def code_from_screenshot(self, image: bytes) -> str:
        # Генерация кода по скриншоту UI
```

#### 28. Agent-as-a-Service
```python
# REST API для внешних приложений
@router.post("/v1/agents/execute")
async def execute_agent(request: AgentRequest) -> AgentResponse:
    """API для IDE плагинов, CI/CD, ботов"""
    return await orchestrator.execute(
        task=request.task,
        agent_type=request.agent,
        context=request.context
    )

# Webhook для уведомлений
@router.post("/v1/webhooks/register")
async def register_webhook(url: str, events: List[str]):
    """Уведомления о завершении задач"""
```

#### 29. Evals и бенчмарки
```python
class AgentEvaluator:
    BENCHMARKS = ["swe-bench", "agent-bench", "human-eval"]
    
    async def evaluate(self, agent: BaseAgent, benchmark: str) -> EvalResult:
        # Автоматическое тестирование на стандартных бенчмарках
        
    async def compare_configs(self, configs: List[Config]) -> ComparisonReport:
        # Сравнение разных конфигураций системы
```

### Phase 3: Продвинутая автономность (1-2 месяца)

#### 30. Agent Swarm (динамическая сборка)
```python
class AgentSwarm:
    async def assemble_for_task(self, task: str) -> Swarm:
        # 1. Декомпозиция задачи на микро-задачи
        # 2. Динамическое создание микро-агентов
        # 3. Координация через shared memory
        # 4. Синтез результатов
        
# Пример: "Создать микросервис с тестами и документацией"
# → Swarm из: Architect, Coder, Tester, DocWriter, Reviewer
swarm = await swarm_builder.assemble_for_task(task)
result = await swarm.execute()  # Параллельная работа
```

#### 31. Tool Discovery & Creation
```python
class ToolGenerator:
    async def discover_api(self, swagger_url: str) -> List[Tool]:
        # 1. Загрузка Swagger/OpenAPI спецификации
        # 2. Генерация Tools для каждого endpoint
        # 3. Валидация и тестирование
        
    async def create_tool(self, description: str) -> Tool:
        # Генерация нового Tool по описанию на естественном языке
```

### 32. Multi-Agent Collaboration (расширение)
**Текущее состояние:** ✅ Базовый `MultiAgentSynthesis`

Расширить для мультиагентных дебатов:
```python
class CollaborativeSolver:
    async def solve(self, task: str, agents: List[str]) -> CollaborativeResult:
        # 1. Каждый агент предлагает решение
        # 2. Критика решений друг друга
        # 3. Синтез лучшего решения
        # 4. Консенсус или голосование
```

### 33. Confidence Estimation
Добавить оценку уверенности к каждому ответу:
```python
@dataclass
class ConfidenceResult:
    answer: str
    confidence: float  # 0.0-1.0
    uncertainty_reasons: List[str]
    needs_verification: bool
```

### 34. Chain-of-Thought Verification
```python
class CoTVerifier:
    """Проверка логических цепочек рассуждений"""
    async def verify_reasoning(self, task: str, solution: str) -> VerificationResult:
        # 1. Разбить solution на логические шаги
        # 2. Проверить каждый шаг
        # 3. Выявить пробелы в рассуждениях
        # 4. Оценить общую логичность
```

### 35. Tool Use Learning
Отслеживать успешность использования инструментов:
```python
class ToolUseTracker:
    async def record_tool_use(self, tool: str, task_type: str, success: bool):
        # Накапливать статистику по использованию инструментов
        
    async def recommend_tools(self, task: str) -> List[ToolRecommendation]:
        # Рекомендовать лучшие инструменты для типа задачи
```

### 36. Визуальный конструктор workflow
GUI для создания workflow без кода:
- Drag & drop агентов и инструментов
- Визуальное соединение шагов
- Условные переходы и циклы
- Экспорт в код

### 37. Межсессионная память о пользователе
**Проблема:** При перезапуске теряется: стиль общения, предпочтения кода (табы/пробелы), часто используемые технологии.  
**Решение:** Создать `UserProfile` (`backend/memory/user_profile.py`) для персонализации промптов  
**Сложность:** Высокая (6-10 часов)

---

## ✅ Исправлено

### Декабрь 2024
- [x] Race conditions в DistributedModelRouter и IntelligentModelRouter
- [x] Небезопасная модификация httpx.AsyncClient
- [x] Утечки ресурсов httpx
- [x] Некорректные SQLite транзакции
- [x] Бесконечная рекурсия в fallback
- [x] Голые `except:` блоки (23 места)
- [x] Магические числа → `constants.py`
- [x] Дублирование markdown extraction → `text_utils.py`
- [x] Типизация `learning_system` в engine.py

### 26 декабря 2025
- [x] Последний голый `except:` в `main.py:310` → типизированные исключения
- [x] Устаревший пакет `duckduckgo_search` → поддержка нового `ddgs`
- [x] Race condition в lazy init `asyncio.Lock` в `IntelligentModelRouter` → `threading.Lock`
- [x] Утечка httpx клиентов в `WebSearchTool` и `APICallTool` → добавлен `shutdown()`
- [x] `ToolRegistry.shutdown()` теперь закрывает все инструменты с ресурсами
- [x] Дублирование сервера в `config.yaml` (gpu_server == base_url)
- [x] `default_model: gemma3` (не существует) → `llama3.2:3b`
- [x] Типизация глобальных переменных `engine` и `preview_manager` → `Optional[...]`
- [x] Захардкоженный таймаут 120s в `chat.py` → `constants.Timeouts.CHAT_TIMEOUT`
- [x] Нерелевантные результаты веб-поиска → добавлен `_filter_relevant_results()`

### 27 декабря 2025 — UX, Производительность, Feedback
- [x] **XSS-уязвимость** — добавлен DOMPurify для санитизации markdown в `MarkdownRenderer.tsx`
- [x] **Feedback UI** — кнопки 👍/👎 полностью интегрированы в ChatMessage
- [x] **Error Boundary** — добавлен `ErrorBoundary.tsx` с красивым fallback UI
- [x] **Централизованные таймауты** — создан `config/timeouts.ts` с TIMEOUT_CONFIG
- [x] **QueryClient с retry** — настроен retry, staleTime, gcTime для react-query
- [x] **React.memo оптимизация** — все компоненты ChatMessage обёрнуты в memo
- [x] **Виртуализация сообщений** — создан `VirtualizedMessageList.tsx` для больших чатов
- [x] **Улучшенные индикаторы загрузки** — добавлены shimmer анимации, пульсирующий спиннер
- [x] **CSS анимации** — добавлены shimmer, pulse-glow, slide-up, scale-in, gradient-rotate
- [x] **LearningIndicator** — компонент для отображения прогресса обучения AI

### 28 декабря 2025 — Архитектурные улучшения
- [x] **UnifiedModelRouter** — консолидация 5 роутеров в единый facade
- [x] **TaskComplexityService** — единая точка анализа сложности задач
- [x] **Streaming в агентах** — добавлен `execute_stream()` в BaseAgent
- [x] **Динамические модели** — SmartModelSelector загружает модели с сервера

### 29 декабря 2025 — UI/UX и Архитектура
- [x] **Модальные подтверждения** — заменены блокирующие `confirm()`/`prompt()` на inline модальные окна
- [x] **ToolsPanel** — `alert()` заменён на UI компоненты ошибок
- [x] **TaskPanel** — ошибки отображаются в интерфейсе вместо console.error
- [x] **CodeEditor** — улучшена типизация ошибок в catch блоках
- [x] **Консолидация типов** — создан `backend/core/types.py` как единый источник истины:
  - `ComplexityLevel` — был дублирован в 2 файлах
  - `ModelTier` — был дублирован в 2 файлах  
  - `ComplexityResult`, `ModelSelection` — общие dataclass'ы
- [x] **Обновлены импорты** — все модули теперь импортируют общие типы из `types.py`

### 29 декабря 2025 — Критические улучшения для качества кода
- [x] **AST-based Code Intelligence** (`backend/project/code_intelligence.py`)
  - Глубокий анализ структуры кода (функции, классы, методы)
  - Построение графа зависимостей
  - Вычисление цикломатической сложности
  - Поддержка Python и JavaScript/TypeScript
  
- [x] **Semantic Code Search** (`backend/rag/semantic_code_search.py`)
  - Семантический поиск по коду на естественном языке
  - Re-ranking результатов через LLM
  - Поиск по паттернам использования
  - Поиск похожего кода
  
- [x] **Incremental Indexer** (`backend/project/incremental_indexer.py`)
  - Отслеживание изменений файлов по hash
  - Индексирование только изменённых файлов
  - Персистентный кэш в SQLite
  - Ускорение работы с большими проектами
  
- [x] **Enhanced Code Validator** (`backend/core/code_validator.py`)
  - Интеграция с ruff для Python
  - Расширенные проверки качества кода
  - Автоматическое исправление через LLM
  - Проверки безопасности (flake8-bandit)
  
- [x] **Self-Consistency Mixin** (`backend/agents/self_consistency_mixin.py`)
  - Генерация N независимых ответов
  - Голосование за консенсус
  - Повышение надёжности для критических задач
  - Оценка уверенности (confidence)
  
- [x] **Multi-Agent Synthesis** (`backend/core/multi_agent_synthesis.py`)
  - Объединение результатов от нескольких агентов
  - Стратегии: merge, select_best, consensus, debate
  - Разрешение конфликтов через LLM
  - Оценка качества синтеза
  
- [x] **Code Intelligence API** (`backend/api/routers/code_intelligence.py`)
  - REST API для анализа проектов
  - Эндпоинты для валидации кода
  - Управление индексами

---

## 📋 Приоритеты реализации (обновлено 29 декабря 2025)

### 🔴 КРИТИЧЕСКИЙ — Phase 1 (следующие 1-2 недели)

| # | Задача | Категория | Время | Эффект |
|---|--------|-----------|-------|--------|
| ~~1~~ | ~~**Гибридный роутер (cost/quality/privacy)**~~ | ~~Роутинг~~ | ~~4-6 ч~~ | ✅ ГОТОВО |
| 2 | **Встроенный тестер кода** | Качество | 6-8 ч | Замкнутый цикл код→тест→исправление |
| 3 | **MCP интеграция (базовая)** | Интеграция | 4-6 ч | Доступ к экосистеме MCP-серверов |
| 4 | **Few-shot из LearningSystem в промпты** | Промпты | 3-4 ч | +15-20% качества за счёт примеров |
| 5 | **Суммирование длинных чатов** | Память | 2-4 ч | Модель не "забывает" контекст |

### 🟠 ВЫСОКИЙ — Phase 2 (недели 3-6)

| # | Задача | Категория | Время | Эффект |
|---|--------|-----------|-------|--------|
| 6 | **Проактивный анализатор** | Автономность | 8-12 ч | Система сама предлагает улучшения |
| 7 | **Мультимодальный RAG** | RAG | 10-15 ч | Анализ диаграмм и скриншотов |
| 8 | **Agent-as-a-Service API** | Интеграция | 6-8 ч | API для IDE, CI/CD, ботов |
| 9 | **Evals фреймворк** | Качество | 6-8 ч | Объективная оценка прогресса |
| 10 | **Интеграция LongTermMemory в Chat** | Память | 4-8 ч | Персонализация + избежание ошибок |

### 🟡 СРЕДНИЙ — Phase 3 (месяц 2+)

| # | Задача | Категория | Время | Эффект |
|---|--------|-----------|-------|--------|
| 11 | **Agent Swarm** | Архитектура | 20-30 ч | Динамическая сборка агентов |
| 12 | **Tool Discovery** | Инструменты | 15-20 ч | Автогенерация Tools по API |
| 13 | **Визуальный конструктор workflow** | UX | 20+ ч | Drag & drop для workflow |
| 14 | **Tree-of-Thought** | Продвинутые | 6-8 ч | Параллельные ветви рассуждений |
| 15 | **Constitutional AI (SafetyChecker)** | Безопасность | 4-6 ч | Проверка на безопасность/этичность |

### 🟢 НИЗКИЙ — по возможности

| # | Задача | Категория | Время |
|---|--------|-----------|-------|
| 16 | i18n локализация | UX | 4-6 ч |
| 17 | Prometheus метрики | DevOps | 4-6 ч |
| 18 | A/B тестирование моделей | DevOps | 4-5 ч |
| 19 | Offline режим (Service Worker) | UX | 6-8 ч |
| 20 | Интеграционные тесты | Качество кода | 8-12 ч |

---

## 📅 Рекомендуемый порядок реализации

### Неделя 1-2: Quick Wins (Phase 1)
- [ ] Гибридный роутер с privacy/cost
- [ ] Встроенный тестер кода
- [ ] MCP интеграция (базовая)
- [ ] Few-shot из LearningSystem

### Неделя 3-4: Интеллект (Phase 2 начало)
- [ ] Проактивный анализатор (базовый)
- [ ] Суммирование длинных чатов
- [ ] Agent-as-a-Service API

### Месяц 2: Мультимодальность и автономность
- [ ] Мультимодальный RAG
- [ ] Evals фреймворк
- [ ] Расширение проактивности

### Месяц 3+: Продвинутые возможности (Phase 3)
- [ ] Agent Swarm
- [ ] Tool Discovery
- [ ] Остальные задачи по приоритету

---

## 💡 Краткий выбор по целям

| Цель | Рекомендуемые идеи |
|------|-------------------|
| **Wow-эффект и автономность** | Проактивность + Agent Swarm |
| **Практическая польза** | Гибридный роутер + Встроенный тестер |
| **Расширение аудитории** | MCP + Agent-as-a-Service |
| **Объективное улучшение** | Evals + Бенчмарки |

---

### ✅ Уже выполнено
- [x] Консолидация роутинга моделей ✅
- [x] Streaming для всех агентов ✅
- [x] Единый TaskComplexityAnalyzer ✅
- [x] Консолидация типов (types.py) ✅
- [x] UI/UX улучшения (модальные окна, ошибки) ✅
- [x] AST-based Code Intelligence ✅
- [x] Semantic Code Search ✅
- [x] Incremental Indexer ✅
- [x] Enhanced Code Validator ✅
- [x] Self-Consistency Mixin ✅
- [x] Multi-Agent Synthesis ✅
- [x] **Гибридный роутер (cost/quality/privacy)** ✅ (29 декабря 2025)
  - Backend: `RoutingPolicy`, `ProviderInfo`, `CostTier` в `backend/core/types.py`
  - Backend: API endpoints в `backend/api/routers/models.py`
  - Frontend: Секция "Политика маршрутизации" в настройках с пресетами
  - Frontend: Индикатор приватности в чате (иконка 🔒/🛡️)
